function wave(str) {
  // Code here
  let newArray = [];
  for (i = 0; i < str.length; i++) {
    //From Marco: Not done yet.
    newArray.push(str[i].toUpperCase());
  }
  return newArray;
}

export default wave;
